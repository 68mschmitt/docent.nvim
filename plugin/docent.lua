-- docent.nvim plugin loader
-- This file is sourced automatically by Neovim's plugin system.
-- The actual setup happens when the user calls require("docent").setup()

-- Prevent double-loading
if vim.g.loaded_docent then
  return
end
vim.g.loaded_docent = 1
